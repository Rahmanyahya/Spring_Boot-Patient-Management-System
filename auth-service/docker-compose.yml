services:
  auth-services:
    build: .
    container_name: "auth-services"
    env_file:
      - .env
    networks:
      - java-project
    restart: unless-stopped

networks:
  java-project:
    external: true
