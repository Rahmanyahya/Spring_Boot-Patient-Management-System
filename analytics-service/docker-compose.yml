services:
  billing-services:
    container_name: "analytics-services"
    build: .
    networks:
      - java-project
    ports:
      - "4003:4000"
    env_file:
      - .env
    restart: unless-stopped

networks:
  java-project:
    external: true